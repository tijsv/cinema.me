<%- contentFor('main') %>

<div class="content">
  <div class="content-inner">

    <% if(!locals.user) { %>

    <h1>Oh hi Mark</h1>

    <p>
      Register for free and follow your favorite series with just a few clicks.
    </p>

    <% } %>

    <% if(locals.user) { %>

    <h2>This is only visible when you're logged in (helps the dev greatly)</h2>

    <div class="searchDiv">
      <h1>Search for movies or series</h3>
      <form id="searchForm">
        <input type="text" id="searchText" placeholder="Search here..." />
      </form>
    </div>

  </div>

</div>

<div id="movies">
</div>

<% } %>

<!-- Waarom zijn deze nodig? -->
</div>
</div>
